const router = require('express').Router();
const {
  // Import methods from controller
} = require('../../controllers/user-controller');

// Set up routes e.g., GET, POST, PUT, DELETE

module.exports = router;
